This is a simple backend code in python that enables you to communicate via texts messages with other computers connected to the same network as the server.py code is run.
Run this in terminal to install requirements pip install -r requirements.txt (Make sure that first python is installed in the system)
After installing the requirements, now run the server.py code by: python server.py OR python3 server.py
